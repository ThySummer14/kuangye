# 旷野 v3 验收记录

日期：2026-09-12。使用本地开发站点的测试存档；测试数据没有写入发布的静态产物。

## 自动检查

`npm test`：15 项通过。覆盖光奖励、每日微光去重、第一件家具和铭牌唯一绑定、旋转占格/碰撞/边界、实付退款、赠礼幂等、v2 迁移、v3 完整往返、损坏 home 清洗、旧货顺序确定性、表情弹簧稳定、真实 store 流程防重复结算、连续任务未达标不能完成、任务上限、损坏 v3 降级读取 v2。

## 浏览器实测

| 流程 | 结果 |
| --- | --- |
| 接一次任务 → 留回顾 → 完成 | 获得 15 光及 25 XP，弹窗显示回顾 |
| 从结算进入商店 → 购买 5 光草席 | 余额由 15 变为 10，出现回家布置入口 |
| 旋转 → 选择格子 → 放置 | 房间可见草席，铭牌包含任务名、回顾与日期 |
| 刷新页面 | 家具数量和摆放位置保留 |
| 375 × 812：购买第二件草席 → 选择重叠格 | 提示已有家具，放置按钮不可用 |
| 选择另一空格 → 放置 → 回收 | 5 光购入返还 3 光，移除摆放与库存，回忆仍在手记 |
| 导入 v2 测试备份 | 完成、进行中与暂时放下记录保留，历史 50 XP 折算 30 光 |
| 导入后连续任务 2/3 → 记录今天 → 完成 | 进度 3/3、奖励 5 光，保持原任务规则 |
| 四张草席与一只小凳共五件家具 | 手机可逐件放置，刷新后全部保留 |
| 直接点击 3D 凳子 → 移动位置 → 点击地面 | 凳子被选中并移动成功 |
| 375px 小屋、商店、手记 | 实测无文档横向溢出；商品缩略图正常加载 |
| 成长线 | 前一阶段完成后的下一阶段可接取，其余阶段锁定 |
| 页面工具 | `get_kuangye_progress` 与 `navigate_kuangye` 已注册；有效输入工作，非法场所被拒绝，读取无副作用 |

桌面检查尺寸 1440 × 1000，手机布局检查尺寸 375 × 812。地图 draw calls 实测 101；五件家具的小屋实测 44（最终尺寸调整不改变几何）。这是渲染调用数，不是实体手机帧率证明。

## 明确的验证限制

- 没有使用实体手机做 45fps 压力测试，也没有验证所有家具把房间填满时的最坏帧率。
- WebGL 不可用/上下文丢失时有场所与平面格子回退，但没有模拟每一种显卡和浏览器故障。
- 浏览器无痕限制、配额不足、跨设备同步不在正常成功路径中；代码会提示保存失败，必须手动备份。
- 成长报告为文本文件，不是截图分享卡；新站点仍使用本地存档，不会自动同步旧网址的数据。
