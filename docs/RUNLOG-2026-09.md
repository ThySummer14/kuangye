# 2026-09 执行记录

## 执行合同
按已批准顺序：去蹦跳 → 地图唯一导航 → 木器铺 → 独立表情试验台验收后集成。每项独立提交，保留 v3 存档和经济规则。最终同步 GitHub / Sites。

## 基线与检查设施
- 基线 `db3eb63`：原样纳入已有 AGENTS 与两份用户需求文档；26 项测试通过。
- 开发服务器 `?qa` 启用独立 `kuangye.qa.v3`，不读取正式或旧版存档；天气请求关闭。
- `window.__KUANGYE__.snapshot()` 读取场所、导航、情绪、房屋及视口；本地 QA 才提供 `reset(name)`。
- 具名场景：`map-navigation`、`woodshop`、`emotion-lab`。截图与状态写入 `output/playwright/`，逐项补充结果。
- 不在生产网址执行测试。独立试验台将有自己的状态钩子和重置操作。

## 1 · 去蹦跳
移除空地点击寻路、随机游走、移动时高度与缩放蹦跳。摸摸只触发表情；音乐互动保留摇摆，去掉上下跳跃。家具主动互动保留平滑靠近定位和坐下/打盹/浇花/读书。
验证：27 项测试通过；新增模拟时间检查静止 20 秒与五类家具动作，余额不变。本地浏览器房间加载无报错，截图 `output/playwright/idle-home-1280.png`。旧引导文案一并清理。

## 2 · 地图唯一导航
移除顶部/底部多入口及地图旁重复入口。子页统一回地图，旧 hash 继续兼容，未知/空 hash 回地图；成长线保留从任务/手记第二击进入。长期规则写入 AGENTS。
手机场所标签曾重叠，增加边缘限制、44px 触区和标签避让。复放脚本第一次因桌面箭头参与 accessible name 导致定位超时，改为稳定场所标识，桌面/375px 四个场所进入返回及无溢出检查全部跑通。
证据：`output/playwright/map-navigation-{1280,375}.png`、`navigation-verification.txt`；命令 `node scripts/run-browser-qa.mjs navigation`。

## 3 · 木器铺
地图新增独立木作坊：木板外墙、窗框、工具台、夹板与堆木；装修相关入口及弹窗从家中移除。店内陈列四块墙样、三款木地板，扩建用方格图展示新增方向。
所有操作仍调用 `expandHome` / `changeHomeDecor`；墙地保持免费，逐次扩建 30/50/70/90/110/130 光。
桌面/375px 浏览器复放：换样免费、取消不扣费、首次扩建余额500→470、刷新保存、双向最大36m²余额20、空余额禁用按钮、家中无师傅入口，全部通过。截图 `output/playwright/woodshop-{1280,375}.png`，状态 `woodshop-verification.txt`。运行 `node scripts/run-browser-qa.mjs woodshop`。

## 4 · 表情试验台先行
设计保留绿色种子、浅肚皮、双叶；白色独立眼形替换旧深色点眼。身体 64 点封闭曲线、左右眼独立高宽/倾斜、嘴形/脸颊/叶片均连续 morph。精确阻尼弹簧保留当前速度，临时切换不从上一状态起点重播。
移植 bloub `closedPath` 和 `eyePoses`（裁去 TypeScript 类型），保留 MIT 到 `app/public/licenses/bloub-MIT.txt`。moodstone 仅参考 mood 分层、约5秒呼吸眨眼和快闭慢开节奏，未移植其代码。
先建立独立 `emotion-lab.html`，不加载 store 或游戏 App。14 情绪、任意起终点、循环、暂停、速度/阻尼、眨眼/呼吸开关与参数读数。29 项测试通过，涵盖196组情绪组合、中途打断、帧步长一致与封闭曲线拓扑；构建成功。初次浏览器检查缺 favicon 404，已补独立图标。
试验台自验收通过（集成前）：桌面/375px 14种连续点选、任意两种过渡、暂停不改变参数、速度/阻尼、循环及眨眼呼吸开关全跑通，无页面异常或溢出。笑眼线过细与负向嘴形不明显，已增粗眼弧并改为正负嘴弧；复放通过。
证据：`emotion-lab-{1280,375}.png`、`emotion-transition-{1280,375}-{1,2,3}.png` 与 `emotion-verification.txt`。截图截取到了持续变化的真实中间参数，没有预渲染替身。接下来使用同一运动与绘制模块集成主站。

## 用户调整 · 暂停小芽形象
用户明确指出眼睛观感不满意，后续交给其他 agent，要求先放下小芽形象。停止第4项继续设计；撤回本轮尚未提交的主站形象/表情集成，保留第1项去蹦跳。独立试验台源码与已有证据保留供交接，但从生产构建入口排除，不发布试验台。此前技术自查通过不等于用户视觉验收通过。

后续 agent 入口：`app/emotion-lab.html`、`app/src/components/EmotionLab.vue`、`SproutPortrait.vue`、`game/emotions.js` 中实验参数、`scenes/sprout-paint.js` / `lab-face-paint.js`。当前眼睛方案未获得用户认可，不应直接接入主站。主站继续沿用原 BuddyFace 与 3D drawFace；实验与原样式绘制已隔离。

## 最终交付检查（按调整后的范围）
- 前三项提交：`3830579` 去蹦跳；`d8e3dde` 地图导航；`4cb9ac5` 木器铺。基线 `db3eb63`，检查设施 `52565b8`。
- 家具主动互动桌面/375px 逐一操作浇花、坐下、打盹、读书、摇摆，五条发现均记录，余额保持500，无页面异常/溢出；证据 `home-interactions-{1280,375}.png` 和 `home-verification.txt`。这是固定本地家具 fixture，不读取用户存档。
- 地图截图与状态已补含木器铺的五场所版本，桌面/375px 主路径均可进入返回。三类具名场景重置入口及截图脚本保留，试验台仅本地使用。
- 已恢复原主站 BuddyFace、3D 小芽及 face-paint，暂停形象实验不影响已完成的去蹦跳。

## 下次该怎么做
1. 形象以用户认可的视觉参考为准。数值连续、测试通过只能证明动画工程正确，不能代替眼睛与整体观感的视觉验收；本次白眼方案不可直接接回。
2. 先在独立试验台展示静态各情绪与中间帧，形象认可后才共用到 2D / 3D。主站原参数与实验参数同在 emotions.js，但消费者和绘制模块已隔离。
3. 导航入口只加地图场所；相应补具名场景与375px标签避让验证。交互测试用稳定场所标识，不依赖桌面/手机显隐不同的装饰箭头。
4. 装修规则继续调用 game/room.js 与 store 门面；样本可扩展但价格不要复制到视图。用空余额、首次、连续扩建到上限、取消、刷新检查真实界面。
5. 页面钩子只读信息可保留；fixture 重置严格限定本地开发 + ?qa，正式 bundle 不得带测试存档或资金重置逻辑。发布前检查试验台没有进入 dist。
最终 `npm test` 29/29 通过；`npm run build` 成功，保留原 three.js 分包大小提示（553.71 kB，非错误）。已检查生产输出无 `emotion-lab.html`、测试存档键和 fixture 重置逻辑。发布只包含前三项功能，实验源码供后续交接。

## 第二轮恢复 · 用户选定02软角（2026-09-19）

用户先批准02概念落地、其余idea归档，随后要求形象更白。本段替代上文“暂停形象”“试验台不得发布”的阶段结论；不回用被否定的白眼方案。概念图与四种轮廓的 morph 评估见 `docs/ideas/mascot-directions.md`。

### 实际实现与取舍

- 身体采用不等高短圆角、融合侧凸、饱满闭合曲面。原燕麦色改为暖白 `#f3eee4`，阴影从棕褐改为中性暖灰；3D增加0.1强度的暖白补亮，仍接受场景的时间光照。
- 外观配置在 `game/mascot.js`，情绪唯一参数在 `game/emotions.js`；独立试验台、BuddyFace、3D统一连续眼形和弹簧运动。现阶段只有02默认皮肤，未实现其他赛季皮肤或商城。
- 3D侧面厚度由独立深度保留；轮廓、正面曲率与五官深度使用同一计算，五官不漂在独立平面上。身体、眼睛、嘴和接触阴影均不依赖贴图。
- 主站接回14情绪。摸摸、眨眼、呼吸、视线跟随及五类家具互动继续保留；没有恢复地面点击游走或蹦跳。
- 地图新增小小画室作为试验台入口，生产构建含 `emotion-lab.html`；平面/立体切换、拖动旋转、任意情绪过渡、循环、暂停和参数调节可用。试验台不导入游戏store。

### 走过的弯路

最初把整个轮廓直接按放射方向鼓起，圆角和侧凸会在脸上形成突兀折线。改为正面使用平滑椭圆基体，轮廓差异逐渐集中到外缘；身体与面部贴合深度同步更新。最初嘴弧过深像尖喙，缩小笑嘴开度和弧高。原暗色渐变让上角与下半身显脏，改为更宽的暖白渐变与中性地面反光。

3D预览切换后第一帧截图曾出现空白，新增实际渲染后的 `data-ready`，脚本等到首帧后再取证。Prettier给可复放脚本加尾分号导致CLI解析失败，runner只裁掉文件末尾分号，保留脚本可格式化。启动本地服务器时根npm脚本未透传参数，改用README明确的 `npm --prefix app run dev -- --host 127.0.0.1 --port 5182 --strictPort`。

### 本次验证与证据

`npm test` 31/31通过，含196对情绪收敛与打断连续性、封闭曲面拓扑和体积、所有表情贴面、无角色贴图，以及经济、存档、碰撞和家具互动回归。`npm run build`成功，three分包533.10kB的大小提示保留，无构建错误。检查生产bundle不含 `kuangye.qa.v3`、`furnished`、`map-navigation` 测试fixture；正式输出包含试验台与bloub许可。

下列均在本地一次性QA存档复放；桌面1280与手机375px，无页面异常或横向溢出，没有访问生产存档。

| 具名场景 / 命令后缀 | 检查结果 | 截图与状态（相对本文件） |
|---|---|---|
| map-navigation / `navigation` | 六个地图场所可进可回，包含独立试验台返回 | [桌面](../output/playwright/map-navigation-1280.png) · [375px](../output/playwright/map-navigation-375.png) · [状态](../output/playwright/navigation-verification.txt) |
| woodshop / `woodshop` | 墙地免费，取消不扣，500→470首扩建，刷新保留，上限与余额不足正确 | [桌面](../output/playwright/woodshop-1280.png) · [375px](../output/playwright/woodshop-375.png) · [状态](../output/playwright/woodshop-verification.txt) |
| furnished home / `home` | 浇花、坐下、打盹、读书、摇摆均记发现；光保持500 | [桌面](../output/playwright/home-interactions-1280.png) · [375px](../output/playwright/home-interactions-375.png) · [状态](../output/playwright/home-verification.txt) |
| emotion-lab / `emotion` | 2D与3D各14情绪、2D暂停/调参/循环、实际连续过渡、3D拖动侧视 | [平面桌面](../output/playwright/emotion-lab-1280.png) · [平面375px](../output/playwright/emotion-lab-375.png) · [立体桌面](../output/playwright/emotion-3d-1280.png) · [立体375px](../output/playwright/emotion-3d-375.png) · [侧面](../output/playwright/emotion-3d-side-1280.png) · [状态](../output/playwright/emotion-verification.txt) |

统一运行 `node scripts/run-browser-qa.mjs <后缀>`。过渡中间帧见 `emotion-transition-{1280,375}-{1,2,3}.png`。人工查看本次桌面/375截图，确认暖白、深色小五官、地图标签与试验台响应布局。旧 `soft-corner-*` 截图是提亮前的探索过程，不能当成本次最终配色。

浏览器仅出现现有three的 `PCFSoftShadowMap` 弃用提示，库会自动转为 `PCFShadowMap`；无运行异常。未做实体手机性能基准；截图复查不等于用户最终审美验收。

### 第三轮只立题

新增 `docs/WORLDVIEW-PROPOSALS-2026-09.md`，提供归光营地、逐光航路、四时来信三案，每项标注现有文档段落或推演。列明等级区域/玩法、四类设施、吉祥物身份及待拍板项。本轮没有实现地图区域解锁、蚀刻章或新任务。小小画室属于第二轮试验台入口，不代表第三轮候选区域已经开工。

### 下次该怎么做（更新）

1. 继续以用户选定概念与实际浏览器观感为准，先检查正面、侧面、夜间再改默认形象；不把测试通过当成用户认可。
2. 修改轮廓时同步体积和贴面深度；所有情绪继续共享 `game/emotions.js`，赛季不能私自复制参数。外观改色不动经济或存档。
3. 稳定状态等首帧/参数条件再截图，保留真实过渡中间帧；不要用固定等待掩盖尚未渲染。
4. 世界观选定之后再写实施规格。未获选择的方案继续归档，不先造区域、不编任务、不加币源。

## 第四轮 · 系列与蚀刻章文档、空骨架（2026-09-19）

用户已选世界观A「归光营地」，其余选择授权agent收敛；本轮明确只交文档与声明骨架，不改界面、不写具体任务。第五轮依赖主题与升级树定稿，本轮未启用。

查看五张用户提供的本地人物参考图。诊断记录在 `docs/MASCOT-EXPRESSION-REVIEW.md`：问题不是只由眼睛大小造成，小眼变细弧、嘴形和眼倾斜/身体变形同步叠加，削弱稳定的脸部识别；后续先比较较大圆钝竖椭圆、弱嘴、克制不对称与独立变量。没有复制参考图到项目，没有修改形象或生成图片。

`docs/SERIES.md`提供8类模板（必选3类＋累计/组合/探索/创造/复盘5类），逐项处理全部候选，明确判定、跨季回归、首次条目与家具铭牌区别、NPC与地图入口、三阶升级、发放预算、空白写作卡、未来存档策略和单独待决分歧清单。一长期family一枚章；素刻/嵌纹/镀彩复用现有难度而不发额外XP或光。默认图鉴位于瞭望台的营地印记柜，核心收藏功能Lv.1可达。

`app/src/data/series.js`与`etchings.js`只导出8个未启用模板、空正式目录与三阶规则声明。不被App/store/场景导入，内容字段为空，没有发放器/迁移器/UI。原任务文件和依赖未修改。

交叉检查发现组合m1的两个组必须消费不同证据，不能只预算一个完成；因此首季工作量从草拟18修正为19（七条单事件m1＋组合两条＋五线各两条进阶）。收集上限为8枚，其中最多5枚嵌纹，首季0枚镀彩；这是目录供给约束，不是隐藏领取配额。正常8–12个事件预估3–5枚、其中1–2嵌纹，明确标为策划估算而非用户数据。

迁移检查发现normalizeState会丢陌生字段和未知qid，importData只赋值已知顶层字段。文档明确未来必须同步更改加载/规范化/导入/导出/初始化与历史目录，不能声称加一份seriesProgress就自动兼容。本轮没有实际迁移、写入生产存档或变更经济。

验证：`npm test` 31/31通过；一次性Node import/assert检查8系列模板、8章模板、3阶、空正式目录、所有模板disabled/draft及零额外奖励通过；`git diff --check`通过。无界面改动，按用户本轮范围不运行浏览器、不重新部署Sites；原线上形象保持原状。本轮为后续作者与实现者提供合同，不声称系列/图鉴已上线。

下次先让用户填family主题和具体任务，再校验引用/预算与升级路线；主题未定不能按八个空类别机械生产八枚章。第五轮按总览先行和逐枚审批推进。角色视觉另开独立比较批，不把“放大眼睛”直接当作已解决。

## 第五轮 · 按冻结参考图更换小芽（2026-09-20）

用户已定稿，本轮不再做概念探索。唯一视觉真值为 `docs/mascot.md` 及其清单中的六张参考图，已逐张读图；旧眼宽倍率试验取消。干净起点是 `6188e40a0f3445857c16152bfb08e777c6d4430c`。形象之外的导航、房屋功能、经济、存档、赛季、剧情、SERIES 与蚀刻章不在本轮范围。

### 执行计划与实际实现

1. 在修改前归档试验台与主站的桌面/375px 截图，再读色板、轮廓、表情消费者与具名场景。
2. `game/mascot.js` 统一奶油白、琥珀金、粉腮红与双叶嫩绿，统一身体径向轮廓、角、芽和手的位置。`softCorner` 标识保留，不改变存档语义。
3. 先在独立试验台实现共享闭合眼形与14情绪，再接同一套2D/3D渲染。弹簧解析解与速度继承保留，嘴固定弱化小w，呼吸/眨眼/视线跟随保留。64px头像也接入试验台调速与阻尼。
4. 以具名 `mascot` 场景复放2D/3D全部情绪、五个视角、日夜与64px；复放原 `emotion` 过渡场景及 `home` 五种家具互动，检查小屏溢出与光余额。最后运行测试、构建并记录证据。

身体是封闭径向几何，下宽上窄、无腿；左角高于右角，体侧圆瘤手没有手指。身体和五官使用 Basic 平涂材质，顶点色只在身体下缘做极浅暗部；嫩芽使用 emissive。没有眼瞳、高光、角色贴图、运行时外部资源或新依赖。柔光用朝向相机的径向透明几何实现 sprite 的观感，避免生成额外纹理。角色含芽光晕为1984三角形，其中本体1884；独立地面接触影另144三角形。地图角色脚底固定，呼吸通过身体形变完成；专注时转身摆幅随呼吸参数连续收敛到零。

### 映射与约束取舍

| 情绪 | 参考与实现 |
| --- | --- |
| idle | 基1，实心竖椭圆眼、自然呆坐 |
| curious | 扩4，微歪头、芽轻竖；始终两叶 |
| happy | 基2/扩6，上弧笑眼、轻抬手 |
| proud | 基8，笑眼与轻微挺胸 |
| celebrate | 扩12，笑眼举手；遵守已确定的不恢复蹦跳规则 |
| sleepy | 基4/基12，眼闭成缝、团子融化，芽随姿态垂下 |
| sad | 基5/扩11，低落眼形与垂芽；不附加泪水贴纸 |
| worried | 扩3，缩小害怕；不使用与之相反的同时拉长动作 |
| shocked | 基6，身体压扁与小点眼 |
| loved | 基10/扩8，笑眼抱拢双手；光罐属于穿戴层，不嵌入基础模型 |
| excited | 基3，实心大眼与竖芽；忽略参考中的空心环眼 |
| thinking | §4提案，半睁凝神与轻偏视线，不增加道具 |
| focused | §4提案，实心眼微收、身体和芽安静；“缩瞳”按无瞳孔铁律解释为整枚眼形微收 |
| shy | 基7/扩1，微不对称眼、脸红收手 |

### 迭代、失败与修正

- 初版大块面部三角形穿入曲面，腮红显得空心。先细分确认原因，再改为沿封闭轮廓成对铺设短三角带并投影中间行，保留实心眼/腮红，也将角色含光晕压回2k以内。
- 初版侧视叶片像薄片且互相重叠。增加真实厚度和前后分叉，正面与侧面都保留双叶体积；放宽左软角、平滑身体半径插值。
- 旧几何断言把顶点0当作脸部最前点；新径向模型顶点0是底部中心。只将该断言改为底部/顶部朝外法线和实际最前点朝外法线；保留闭合边计数、厚度、高度、全部情绪贴面深度及无贴图断言，并增加双角高低、双叶、双手、实测配色断言。没有为通过测试降低视觉标准。
- 2D拥抱的手原先画在五官下面，与3D遮挡关系不同；将前景圆瘤手画在面部之后，避免腮红浮在手上。
- 低落和思考最初把竖椭圆压成了近圆形，倾斜也因此难以察觉；收紧其眼高，使低落眼的内外倾斜与思考半睁更可辨，仍沿同一实心轮廓连续变形。
- 日夜场景首次复放时，原生label包含选项文本，严格匹配“光线”失败；改用已有光线label前缀。手机截图受页面滚动影响截到页首；QA临时关闭平滑滚动并拍完整 `.home-stage`，不改产品布局。
- 人工逐图检查发现，仅等弹簧速度较小会误把上一情绪的静止帧当作新情绪。改为同时等待目标mood已生效、每个参数距目标小于0.005与速度收敛，再重新拍全部情绪。终态数值审查进一步发现弹簧拐点处速度虽小，角度仍有约0.07度余量，因此额外暴露target并检查目标距离，保留真实弹簧、不把它硬吸附到终点。夜间试验台的按钮增加浅色底，解决暗字与暗背景对比不足。

### 可复放证据

本轮截图目录为 `output/playwright/mascot-v1/`。`before/` 是改造前；`initial-*`、`check-*`、`refined-*` 是迭代记录，不是最终交付图。最终按 `{2d,3d}-{mood}-{width}.png` 命名；桌面记录全部14情绪，375px逐一检查14情绪并截idle/shocked/sleepy/excited及五视角。使用一次性 `?qa` 存档，不读取生产存档。

| 检查 | 桌面证据 | 375px证据 |
| --- | --- | --- |
| 改造前实验室/主站 | [试验台](../output/playwright/mascot-v1/before/lab-1280.png) / [小家](../output/playwright/mascot-v1/before/home-1280.png) | [试验台](../output/playwright/mascot-v1/before/lab-375.png) / [小家](../output/playwright/mascot-v1/before/home-375.png) |
| 64px与正常2D | [64px](../output/playwright/mascot-v1/avatar64-idle-1280.png) / [立绘](../output/playwright/mascot-v1/2d-idle-1280.png) | [64px](../output/playwright/mascot-v1/avatar64-idle-375.png) / [立绘](../output/playwright/mascot-v1/2d-idle-375.png) |
| 3D正面/侧面 | [正面](../output/playwright/mascot-v1/turnaround-front-1280.png) / [侧面](../output/playwright/mascot-v1/turnaround-side-1280.png) | [正面](../output/playwright/mascot-v1/turnaround-front-375.png) / [侧面](../output/playwright/mascot-v1/turnaround-side-375.png) |
| 试验台夜景 | [夜景](../output/playwright/mascot-v1/3d-night-1280.png) | [夜景](../output/playwright/mascot-v1/3d-night-375.png) |
| 小家日夜 | [日](../output/playwright/mascot-v1/home-day-1280.png) / [夜](../output/playwright/mascot-v1/home-night-1280.png) | [日](../output/playwright/mascot-v1/home-day-375.png) / [夜](../output/playwright/mascot-v1/home-night-375.png) |

复放命令：`node scripts/run-browser-qa.mjs mascot`、`emotion`、`home`，默认本地Vite端口5182。状态分别记录在 [mascot-verification.txt](../output/playwright/mascot-verification.txt)、[emotion-verification.txt](../output/playwright/emotion-verification.txt)、[home-verification.txt](../output/playwright/home-verification.txt)。具名场景检查56次情绪终态、双叶/双手/不对称角、面数、暂停调参不漂移、64px真实CSS尺寸与日夜小家状态。原home场景的浇花、坐下、打盹、读书、摇摆均保留，光从500到500。

`npm test` 31/31通过，`npm run build`成功。构建保留现有three分包超过500kB提示；浏览器有现有PCFSoftShadowMap弃用警告，无pageerror。没有做实体手机性能基准。截图与状态是实现自查，不替代用户对定稿还原程度的最终审美评价。

### 下次该怎么做

先读冻结真值和本轮最终截图，再改形象。共用色板、轮廓、贴面深度和情绪参数，不在2D/3D另写一套。曲面上的实心五官须检查中间三角形是否穿模，不能只检查轮廓顶点。截图必须同时验证目标情绪与稳定状态，且人工看实际像素；数量齐全不代表画面有效。新道具放穿戴层，后续蚀刻章按第六轮流程另行推进。

## 第六轮 · 蚀刻章编号概念总览（2026-09-20）

第五轮已部署到Sites版本6，GitHub/Sites源对应 `41eaa9c32ba1646fe38cda636e124d4f9f169649`。用户明确开始第六轮，要求先编号总览、逐枚确认后再实现。

核对 `SERIES.md` 后发现：三阶规则与一family一章已有设计基线，但首批family名称/意义尚空，正式目录也仍为空。已向用户明确这一差别。本轮按新指令先提供8条美术候选主题，全部标为推演和pending，以总览同时评审物象及主题；没有假称八个系列已定稿，没有代写任务，也没有改动SERIES、声明骨架或运行时。

交付 [第六轮评审说明](ETCHINGS-ART-REVIEW-2026-09.md)、[编号总览v2](art/etchings-overview-v2.png)、[完整提示词](art/etchings-overview-prompt.md)。使用内置image_gen两次：初版建立8种独立轮廓与三阶手作材质；复查后定向修改01/02/05/07的中阶结构，让终阶不只依靠颜色。v1保留作对照。共有24个形态在一张总览中，尚不是24张独立资产。

按用户规定的评审顺序，当前停在可评审总览；用户按编号反馈后再出N×3独立图与瞭望台印记柜场景，逐枚确认后才分批实现站内资产。美术候选不写入线上页面，因此本轮无需重新构建或部署站点。文件仅为文档与PNG，无运行时改动；不重复跑界面测试。下次先读取本轮逐枚反馈，确认实际主题与N，再细化；不把总览图直接裁切当成低分辨率正式资产。

## 第六轮交付与一阶段收尾（2026-09-21）

用户认可两张总览，明确“其他自主抉择持续推进”，本轮将该授权用于8枚三阶细化与站内实现。第五轮形象保持冻结。维护使命、Luna/max机械委派与开放内容起草权限已同步CHARTER；ROADMAP恢复为唯一断点入口。

### 实现与审美迭代

24张独立原创图与一张陈列概念场景存于 [图册](art/etchings/README.md)。没有将总览裁成正式图。大尺寸采用本地WebP，24张合计1,032,014 bytes；32px印记使用简化程序化SVG，导出24份供后续使用。运行时无第三方图片、模型或字体下载依赖；cwebp仅为本地资产处理工具，未增加npm依赖。

初版纯SVG大图的雕刻质感不及已批准概念，因此改为大图WebP、小图SVG，保留可辨认的外形。逐图核验发现07素刻出现高起器皿、镀彩口沿有断口，重新生成修正为浅刻椭圆与完整器皿金缝；原图存iterations，未拿旧图作最终截图。场景概念用于美术方向说明，不冒充产品截图。

瞭望台内的营地印记柜从地图两击可达；三阶与8枚详情按01–02、03–05、06–08分批复放。明确展示“图样预览，尚未开放领取”；正式任务/章目录仍为空，不假授章、不新增光、不改v3字段。完成此美术交付后不再继续扩展纯逛展玩法，下一切片回到真实任务行动。

### 验收发现与修复

- 关闭原生dialog时，页面还处于inert状态，焦点恢复失败。现在先close再focus，8枚详情两种宽度共16次均恢复触发按钮焦点。
- 导入清洗会滤掉陌生任务。增加共享parseImport预检，未知任务或未来版本整份拒绝，当前存档保持原样；已知v1/v2/v3沿原清洗路径。补3条真实回归测试，没有放宽旧断言。
- 写入失败提示在恢复可保存后仍残留，成功写入时清空提示。

### 可复放证据

所有浏览器测试使用localhost与一次性`?qa`存档，不访问生产数据。Luna/max执行例行测试与截图，主代理亲自核对结果、源码差异与实际像素。

| 场景 | 状态证据 | 代表截图 |
| --- | --- | --- |
| 地图导航 | [navigation](../output/playwright/navigation-verification.txt) | [桌面](../output/playwright/map-navigation-1280.png)、[375px](../output/playwright/map-navigation-375.png) |
| 木器铺扩建/装饰 | [woodshop](../output/playwright/woodshop-verification.txt) | [桌面](../output/playwright/woodshop-1280.png) |
| 家具五种互动 | [home](../output/playwright/home-verification.txt) | [手机](../output/playwright/home-interactions-375.png) |
| 14情绪/双叶双角/日夜 | [mascot](../output/playwright/mascot-verification.txt) | [日桌面](../output/playwright/mascot-v1/home-day-1280.png)、[夜手机](../output/playwright/mascot-v1/home-night-375.png) |
| 印记柜6批与16次详情 | [etchings](../output/playwright/etchings-verification.txt) | [镀彩桌面](../output/playwright/etchings-镀彩-1280.png)、[素刻手机](../output/playwright/etchings-素刻-375.png)、[07三阶](../output/playwright/etching-7-375.png) |

复放使用`node scripts/run-browser-qa.mjs <场景>`，先以Playwright CLI启动`kuangye-batch`会话并打开本地5182端口。脚本遇到断言错误返回非零。当前单元测试34/34通过，见 [测试记录](../output/playwright/npm-test-verification.txt)。最终构建及完整任务闭环结果在本节交付补记记录。

### 下次该怎么做

图样、实际持有、奖励规则必须分层；看图不应改变存档。资产验收先检查物象结构，再看材质，不把金色当升级。Modal焦点与备份保全必须进入主路径回归。内容测试继续用临时存档，不能用生产经历做fixture。继承既有bloub思路参考但未复制实现或资产；本轮新增美术为原创生成与程序化绘制，未吸收外部仓库代码。

已按官方资料整理 [App Store准备](PUBLISHING.md)，未安装原生依赖、未注册账号或付款。商用前天气API许可、真机性能、原生可靠存档与用户定价仍是发布门槛；浏览器截图不替代原生设备验收。现有three分包体积及PCFSoftShadowMap弃用警告仍待后续有针对性处理，不影响本轮主路径测试。

同步告知记录：依据持续授权，本轮验收通过后推送GitHub main并更新现有私有Sites，不改变访问范围。

### 一阶段验收补记

构建成功，见 [build](../output/playwright/build-verification.txt)；构建中的24张WebP及manifest逐文件SHA256与源一致。完整 [journey](../output/playwright/journey-verification.txt) 在1280/375均通过：空档接取既有慢跑任务→写回顾并完成获得15光→购5光草席→摆入小家→手记导出真实Blob内容并原样导入→记录、铭牌、家具各1份，余额10光→拒绝未来版本与陌生任务且存档不变→返回地图。无pageerror。截图：[完成手机](../output/playwright/journey-complete-375.png)、[小家桌面](../output/playwright/journey-home-1280.png)、[手记手机](../output/playwright/journey-journal-375.png)。

主代理审脚本发现初稿只验证下载事件后从localStorage重组备份，且FAIL报告未抛异常，不能证明真实导出链路。已接管改为读取实际生成JSON Blob并验回顾字段，失败抛错，使runner非零；重新复放通过。首次小家截图因点击后滚动截断顶部，改fullPage后重新留证；不修图、不隐去失败。

一阶段自评：美术交付、只读陈列与既有任务生活闭环已跑通，用户经历的导出保全更可靠。这里没有宣称系列授章或原生App已经完成。下一次按ROADMAP推进现实行动路径，而不是继续增加产品内的装饰游览。

最后手机像素复查发现家具互动按钮只占网格第一列，导致“坐下来晒太阳”单字换行；让直接互动按钮跨两列，完整journey再次在桌面/375px通过。继续会话时本地预览进程已退出，使用`npm --prefix app run dev -- --host 127.0.0.1 --port 5182`重启；根npm脚本不透传这些参数，避免再误启动错误root。

### 同步交付回执

2026-09-21：GitHub main与Sites源均已成功推送交付提交 `35796037aaeeeecc7ed1ffaf59c82195e8e59a2b`。Sites保存为版本7；部署 `appgdep_6ab0d9a560808191b6675eb8729a6dae` 原生结果为succeeded，网址为 https://kuangye-sprout-home.thaotuyetbauqciloc.chatgpt.site 。保留既有私有访问范围。构建包37文件、1,914,880 bytes，仅含部署产物。

本回执作为后续纯文档提交同步GitHub；线上运行时代码仍精确对应上述交付提交，不为记录回执重复部署。下一步按ROADMAP审查首次选任务与行动路径，工作区收尾保持干净。

## 自主维护第一切片 · 找到一件事并开始（2026-09-21）

用户再次确认全权维护使命。按ROADMAP从行动入口接续，不新增装饰场所。开工工作区干净；先提交 [行动入口契约](ACTION-ENTRY.md)（51909a0），再改实现。

发现与决定：原本query虽然跳过了推荐排序，却没有跳过today的E/D筛选，导致准确输入较高难度任务标题也找不到。将选择逻辑提取为纯函数selectTasks；有非空搜索时检索全难度，保留领域、已接取、完成不可重复等边界。成长线前置与接取上限仍由canAccept判定，搜索可见不代表已经解锁。

接取后DOM中的原卡消失、新卡出现在上方，手机会留在旧位置；现在nextTick后聚焦对应在手任务并移到视口，免去向上找卡。操作指引提醒在生活中做完再记录；累计进度和输入占位使用既有unit字段。空结果提供清空筛选，动作结束聚焦搜索框，防止按钮消失后丢失键盘落点。主代理375px复核看到页标题旁提示句被max-width100px挤成末行单字，仅在本组件取消该限制。

不新增任务、经济入口、存档字段或依赖；本轮未借鉴外部代码/资产。两个新增纯函数回归检查覆盖高阶标题/空白搜索、领域与历史记录约束。实际用户行动率尚无测量，本轮只证明入口交互可达、可见、不会越过资格。

Luna/max完成例行测试构建及行动场景首稿，主代理亲自核对36/36通过和构建exit 0，并复查源码与截图。首稿isVisible不等于卡片进入视口，补充真实boundingBox边界断言；修正错误分支在Node侧引用document的问题。截图中的旧toast造成状态歧义，改为等待它自然消失后拍摄，不隐藏DOM。会话接续时本地服务退出产生connection refused，重新启动后重跑；action-fail截图保留为环境失败证据，最终以action-search/accepted/accumulated为准。

复放方式：启动本地5182 Vite及Playwright会话；默认runner使用kuangye-batch，可通过`KUANGYE_QA_SESSION=kuangye-action`选择隔离会话。`action-entry`在1280/375验证锁定任务可查但不可接、空结果恢复与搜索回焦、接取卡完整进入视口且focus正确、累计单位、光保持0和无溢出。状态见 [action-entry](../output/playwright/action-entry-verification.txt)，画面见 [搜索375](../output/playwright/action-search-375.png)、[接取375](../output/playwright/action-accepted-375.png)、[累计桌面](../output/playwright/action-accumulated-1280.png)。[journey](../output/playwright/journey-verification.txt)再次通过，恢复后余额10、完成/家具/摆放各1。日夜与14情绪沿用mascot具名场景，最终结果随后记录。

本切片按持续授权在最终验收后推送GitHub main、更新既有私有Sites。下次先检查CompleteModal的购物主按钮与自然结束动作，再进入离线原生包验证；不继续无边界地做装饰微调。

最终验收：mascot场景exit 0，14情绪×2D/3D×两宽度及日夜均通过、无pageerror；主代理亲自查看新 [夜375](../output/playwright/mascot-v1/home-night-375.png) 与 [日1280](../output/playwright/mascot-v1/home-day-1280.png)。测试36/36、build成功；保留既有three大分包及阴影API弃用警告，无新增运行时错误。小芽几何与存档经济未改。此为完整行动入口切片验收，不代表真实行动率或原生性能已验证。

交付回执：2026-09-21，GitHub与Sites源成功推送 `8985637f44ec1dee2620dfdda773b7577a2cdc04`，Sites版本8部署 `appgdep_6ab124cd77108191ac884c4c4ceb03f2` 返回succeeded；网址 https://kuangye-sprout-home.thaotuyetbauqciloc.chatgpt.site 。访问范围未变。本回执另作纯文档提交，线上代码对应上述SHA；不为回执重复部署。

## 自主维护第二切片 · 完成后可以安心结束（2026-09-21）

按 ROADMAP 接续 CompleteModal。开工工作区干净，基线 fa27214。短契约见 [COMPLETION-EXIT](COMPLETION-EXIT.md)。原结果页唯一主按钮是购物，而“先把这一刻记下来”容易让人以为确认完成后还没保存。改为“收好了，回到地图”作为唯一主动作；购物和符合原资格的成长线仍可选。此处不新增奖励、不改变任务内容，不把完成现实行动转换成产品内新待办。

完成后焦点落到结束按钮，返回地图后落到品牌地图按钮，防止原任务卡消失后键盘焦点丢失。显示记入手记的提示，但读取既有 saveWarning：写入失败就在弹窗内显示真实警告，不能用成功文案掩盖失败。存储、经济、成长线规则与小芽几何均未修改；没有新增依赖或借鉴外部代码/资产。

主代理独立复放375×812空回顾完成页，亲看 [手机完成页](../output/playwright/completion-review-375.png)：小芽、奖励、主按钮与次要动作完整可见。主动作回地图后焦点为brand；刷新仍完成1条、15光。初次模板编辑出现多余闭合标签，diff审阅立即修正后再验收；未跳过失败或修图。Luna/max执行例行验收，测试日志实际36/36、无跳过，build成功；最终浏览器矩阵与提交结果在下段补记。

本轮范围为本地完整切片及commit，尚未更新Sites；线上仍为版本8。ROADMAP清除更早版本7的重复断点，下一技术动作固定为离线原生包/持久存档原型；PUBLISHING继续累计发布门槛，不将浏览器通过冒充原生验收。

像素复核补正：失败状态原主按钮仍写“收好了”，与警告矛盾，改为失败时只写“回到地图”。为此重新跑全测试与build，仍36/36及exit 0。子代理完成测试构建，但浏览器例行任务迟迟未交回，因此主代理接管脚本和截图。一次在复放中修改文案使旧断言超时；随后发现仅变更hash不会重建页面，旧modal会遮住新fixture地图。现脚本每组fresh显式reload，再reset，隔离临时Storage拦截与弹窗。失败日志分别保留completion-hmr-interruption及completion-fixture-retry，最终以completion-exit-verification为准。


最终验收与收尾（2026-09-22接续）：主代理亲核 [completion-exit](../output/playwright/completion-exit-verification.txt) 两宽度均PASS、无pageerror：自然结束与焦点、刷新保留回顾/一条记录/15光、Escape不重发、成长线可选、QuotaExceededError真实提示，桌面/手机日夜截图均已逐张查看。保存失败主按钮已改为“回到地图”，手机像素复核无矛盾。现有 [journey](../output/playwright/journey-verification.txt) 购物→铭牌→摆放→实际Blob备份恢复→拒绝不兼容档闭环两宽度均通过，余额10光且完成/家具/摆放各1。最终 [test](../output/playwright/completion-test.txt) 36/36， [build](../output/playwright/completion-build.txt) 成功；既有three大包和阴影API警告保留。本轮仅本地交付，未部署、未操作账号与费用。下一步已写入ROADMAP：离线原生包与可靠存档。

## 自主维护第三切片 · 原生存档在真实模拟器上闭环（2026-09-25）

接续 ROADMAP 的离线原生包切片。开工时工作区已有未提交的整片实现（Capacitor 8.5.2、iOS 工程、SaveFiles/KuangyeStoragePlugin、storage/persistence/native 三层、9 条故障注入测试），上一会话停在了"逻辑与构建完成、模拟器 runtime 下载到 5.2% 中断"。本轮不重做，只收口：补齐缺失的实机验证，把发现的问题修掉，commit。

### 实机验证与两处修复

- 装好 iOS 27.0 runtime（8.05GB，`xcodebuild -downloadPlatform iOS`，磁盘曾不足，95Gi 可用后成功）。新建 kuangye-verify 模拟器（UDID 记于 output/native/sim-device.txt）。
- 首次启动即抓到真实缺陷：`"KuangyeStorage" plugin is not implemented`，启动错误屏如实出现且未覆盖文件（失败保护按设计工作）。根因：Capacitor 模板 SceneDelegate 用代码硬建朴素 `CAPBridgeViewController()`，绕过了 Storyboard 中注册插件的 `AppViewController.capacitorDidLoad`。官方 custom-code/viewcontroller 文档确认注册钩子挂在自定义 VC 上。修复：SceneDelegate 从 Main.storyboard 实例化初始 VC。
- 第二处：`main.js` 曾让全部平台 await 文件恢复后再动态 import 挂载，Web 启动变成与模块加载竞速，QA 脚本 reload 后取 `__KUANGYE__` 失败。改为 Web 同步挂载（与历史行为一致），仅原生平台 await 恢复后挂载。教训：把 Web 顺便拖进为原生设计的异步路径，是在给不变的东西引入变数。
- 本机 GUI 自动化不可用（无障碍权限关闭、合成鼠标事件被拒、无 idb/appium），改用 XCUITest（在模拟器内执行，不需要宿主权限）：把测试 target 注入 App.xcodeproj（python 脚本改 pbxproj，`cap sync` 实测不改写它），scheme `KuangyeTests` 可复放。

### 验证链与证据

XCUITest 驱动真实 App 完成闭环：预置 347 光的 `current.json`（WebView localStorage 为空，余额只能来自原生插件读取）→ 点任务岩壁 → 完成慢跑任务 → 362 光 → 终止进程重启 → 362 光完整恢复。完成后磁盘核对：`current.json` 362 光、done 含 run-s1；`previous.json` 保留 347 上一代。截图 01-launch/02-after-fix/03-native-read/01-modal/02-result/03-after-relaunch 在 output/native/sim/，测试在 app/ios/App/AppUITests/NativeSaveTests.swift，运行日志 output/native/xctest-run*.txt。中途失败轮次的截图也保留（03-missing-checkin 帮助定位到 once 任务没有"记录今天"按钮，改走完成路径）。

Web 回归：journey 与 completion-exit 场景 1280/375 双宽度 PASS、无 pageerror（备份导出真实 Blob、拒绝未来版本与陌生任务、完成结算 15 光）；mascot 场景日夜截图重拍，日景暖白木质、夜景窗光收敛，无 AI 默认风格命中。`npm test` 44/44，`npm run build` 成功。

### 记账与边界

- 本轮为原生切片新增 Xcode 工程、Capacitor 依赖（MIT）、AppUITests 与 scheme；`app/ios/.gitignore` 排除 public/ 等生成物。无新增美术资产，无外部代码吸收（SceneDelegate 修复依据官方文档行为，未复制实现）。
- 仍未做、不得冒充已完成：真机签名包与性能、真机 WebView→原生文件的就地迁移演练（逻辑有测试，设备级未验）、系统分享/原生备份导入导出（留下一切片）。Sites 未更新，线上仍为版本 8。
- 下一切片按 ROADMAP：起草首批事件型系列内容给用户评审，或做原生备份导出切片。

## 自主维护第四切片 · 原生备份可带走、可找回（2026-09-25）

接续上一切片。目标：原生存档的用户自救路径——导出走系统分享与"文件"，导入走文件选择器，格式与 Web 完全同一条 `parseImport` 管线。

### 实现与验证

- `KuangyeBackupPlugin.swift`：`exportBackup` 原子写入 App 容器 Documents（Info.plist 开启 UIFileSharingEnabled，用户在"文件"App 可见）后弹出分享面板；`importBackup` 用 `UIDocumentPickerViewController(forOpeningContentTypes:[.json], asCopy:true)` 加安全作用域读取。注册进 AppViewController 的 `capacitorDidLoad`。
- `services/backup.js`：非原生环境是惰性空桥（Node 测试证明：导出返回 false、导入返回 null，调用方回退 Web 的 Blob 下载与 file input），格式分叉为零。
- JournalView：原生导出提示文案改为"备份已存入「文件」"；恢复备份前的自动导出在原生上走静默写（`interactive:false`），不弹分享打断流程。
- 可复放验收固定为 `scripts/native/run-sim-tests.sh`：XCUITest 自我播种（见下）→ 完成任务 → 杀进程重启恢复 → 宿主侧核验 current=362/previous=347 → 手记导出 → 宿主侧核验 Documents 内 envelope version 3 与余额一致。分享面板截图 output/native/sim/04-share-sheet.png（"kuangye-2026-09-25 JSON · 955 字节"，Copy/保存到"文件"可点）。

### 走的弯路与三个教训

1. **store.js 在模块求值时快照状态**：曾把 Web 启动改回同步（静态 import App.vue）以修 QA 竞速，结果原生驱动挂上之前 store 已经取完空档——读取成功、界面为空，非常像"读取失败"。正确解是统一异步启动 + QA 脚本显式 `waitForFunction(() => window.__KUANGYE__)`。教训：为平台差异寻找对称解之前，先找不变量的准确位置（不变量是"驱动先于 store 求值就位"，不是"挂载要同步"）。
2. **xcodebuild test 每次重装并轮换数据容器**：宿主侧播种必然被删；XCUITest 在测试进程内按容器元数据自我播种才稳定。且每次 test 后容器 UUID 都变，宿主侧核验必须重新 `get_app_container`。
3. **模拟器 GUI/宿主输入自动化全不可用**（无障碍权限、合成鼠标事件、idb/appium 均无），XCUITest 是唯一可行驱动；其失败截图与 debugDescription 足以定位 UI 假设错误（once 任务没有"记录今天"按钮、手记入口在地图 POI 而非任务墙内）。

Web 回归 journey/completion-exit/navigation 双宽度 PASS、无 pageerror；45/45 测试、build 绿。Sites 未动，线上仍为版本 8。

### 补记：导入往返也在模拟器上打通（同日）

导入演练从"未做"变为"通过"。过程推翻了我对难点的预估：真正的障碍不是选择器不可自动化，而是三件小事——iOS 26 分享面板没有取消按钮（点击面板上方+下滑关闭）；`UIDocumentPickerViewController` 加 `directoryURL` 直达 Documents 同时解决了用户路径和测试路径（不必在 Files 远程视图里导航）；选择器文件行在无障碍树里不是 button 而是 cell，按类型匹配即可点选。最终 XCUITest 完整往返：导出→分享面板→关闭→导入→选择器点选 `kuangye-*.json`→共享 parseImport 解析→恢复确认卡（"此备份含 1 条完成记录…"）→toast"备份已恢复，原进度也已自动导出"。截图 output/native/sim/05-picker、07-import-confirm、08-import-done。顺带修掉截图暴露的底部文案残留（"你的进度保存在此浏览器"在原生应为"这台设备"）。教训：先让真实截图告诉你障碍是什么，再决定绕不绕。

## 自主维护第五切片 · 首季系列内容提案（2026-09-25）

原生侧收尾（推送分支、合并 main `fa27214..ea5289c`）后，按 ROADMAP 起草首批事件型系列内容。交付 [SERIES-SEASON1-REVIEW](SERIES-SEASON1-REVIEW-2026-09.md)：8 条主题线对准已批准的 8 族蚀刻章美术（01 限时/02 阶段/03 1st/04 累计/05 组合/06 探索/07 创造/08 复盘），写作卡按 SERIES §4 模板填写，19 条任务与预算一致（9 核心 + 10 深化；01/05/08 首季仅素刻）。

起草中的取舍：新 chain 只建一个（hike），其余族复用既有 METRICS/DIFF/领域，避免第二套技能树或货币；所有证据保持诚实自述边界（无拍照/定位/社交认证）；03 的历史首次补录不补 XP/光；05 的"7 天窗口 ≥5 顿"用窗口计数而非连击，绕开断签惩罚的形状。df-s1（独自过夜旅行）定 A 级是最重的一处判断，连同主题方向、口吻、组合难度一起列为待用户裁决点——这四点是本切片声明的合法等待项。

本轮零运行时改动（仅文档与 git 提交），不部署、不重跑浏览器矩阵；45/45 测试与验证链不受影响。
