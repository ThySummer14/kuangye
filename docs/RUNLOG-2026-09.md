# 2026-09 执行记录

## 执行合同
按已批准顺序：去蹦跳 → 地图唯一导航 → 木器铺 → 独立表情试验台验收后集成。每项独立提交，保留 v3 存档和经济规则。最终同步 GitHub / Sites。

## 基线与检查设施
- 基线 `db3eb63`：原样纳入已有 AGENTS 与两份用户需求文档；26 项测试通过。
- 开发服务器 `?qa` 启用独立 `kuangye.qa.v3`，不读取正式或旧版存档；天气请求关闭。
- `window.__KUANGYE__.snapshot()` 读取场所、导航、情绪、房屋及视口；本地 QA 才提供 `reset(name)`。
- 具名场景：`map-navigation`、`woodshop`、`emotion-lab`。截图与状态写入 `output/playwright/`，逐项补充结果。
- 不在生产网址执行测试。独立试验台将有自己的状态钩子和重置操作。

## 1 · 去蹦跳
移除空地点击寻路、随机游走、移动时高度与缩放蹦跳。摸摸只触发表情；音乐互动保留摇摆，去掉上下跳跃。家具主动互动保留平滑靠近定位和坐下/打盹/浇花/读书。
验证：27 项测试通过；新增模拟时间检查静止 20 秒与五类家具动作，余额不变。本地浏览器房间加载无报错，截图 `output/playwright/idle-home-1280.png`。旧引导文案一并清理。

## 2 · 地图唯一导航
移除顶部/底部多入口及地图旁重复入口。子页统一回地图，旧 hash 继续兼容，未知/空 hash 回地图；成长线保留从任务/手记第二击进入。长期规则写入 AGENTS。
手机场所标签曾重叠，增加边缘限制、44px 触区和标签避让。复放脚本第一次因桌面箭头参与 accessible name 导致定位超时，改为稳定场所标识，桌面/375px 四个场所进入返回及无溢出检查全部跑通。
证据：`output/playwright/map-navigation-{1280,375}.png`、`navigation-verification.txt`；命令 `node scripts/run-browser-qa.mjs navigation`。

## 3 · 木器铺
地图新增独立木作坊：木板外墙、窗框、工具台、夹板与堆木；装修相关入口及弹窗从家中移除。店内陈列四块墙样、三款木地板，扩建用方格图展示新增方向。
所有操作仍调用 `expandHome` / `changeHomeDecor`；墙地保持免费，逐次扩建 30/50/70/90/110/130 光。
桌面/375px 浏览器复放：换样免费、取消不扣费、首次扩建余额500→470、刷新保存、双向最大36m²余额20、空余额禁用按钮、家中无师傅入口，全部通过。截图 `output/playwright/woodshop-{1280,375}.png`，状态 `woodshop-verification.txt`。运行 `node scripts/run-browser-qa.mjs woodshop`。

## 4 · 表情试验台先行
设计保留绿色种子、浅肚皮、双叶；白色独立眼形替换旧深色点眼。身体 64 点封闭曲线、左右眼独立高宽/倾斜、嘴形/脸颊/叶片均连续 morph。精确阻尼弹簧保留当前速度，临时切换不从上一状态起点重播。
移植 bloub `closedPath` 和 `eyePoses`（裁去 TypeScript 类型），保留 MIT 到 `app/public/licenses/bloub-MIT.txt`。moodstone 仅参考 mood 分层、约5秒呼吸眨眼和快闭慢开节奏，未移植其代码。
先建立独立 `emotion-lab.html`，不加载 store 或游戏 App。14 情绪、任意起终点、循环、暂停、速度/阻尼、眨眼/呼吸开关与参数读数。29 项测试通过，涵盖196组情绪组合、中途打断、帧步长一致与封闭曲线拓扑；构建成功。初次浏览器检查缺 favicon 404，已补独立图标。
试验台自验收通过（集成前）：桌面/375px 14种连续点选、任意两种过渡、暂停不改变参数、速度/阻尼、循环及眨眼呼吸开关全跑通，无页面异常或溢出。笑眼线过细与负向嘴形不明显，已增粗眼弧并改为正负嘴弧；复放通过。
证据：`emotion-lab-{1280,375}.png`、`emotion-transition-{1280,375}-{1,2,3}.png` 与 `emotion-verification.txt`。截图截取到了持续变化的真实中间参数，没有预渲染替身。接下来使用同一运动与绘制模块集成主站。

## 用户调整 · 暂停小芽形象
用户明确指出眼睛观感不满意，后续交给其他 agent，要求先放下小芽形象。停止第4项继续设计；撤回本轮尚未提交的主站形象/表情集成，保留第1项去蹦跳。独立试验台源码与已有证据保留供交接，但从生产构建入口排除，不发布试验台。此前技术自查通过不等于用户视觉验收通过。

后续 agent 入口：`app/emotion-lab.html`、`app/src/components/EmotionLab.vue`、`SproutPortrait.vue`、`game/emotions.js` 中实验参数、`scenes/sprout-paint.js` / `lab-face-paint.js`。当前眼睛方案未获得用户认可，不应直接接入主站。主站继续沿用原 BuddyFace 与 3D drawFace；实验与原样式绘制已隔离。

## 最终交付检查（按调整后的范围）
- 前三项提交：`3830579` 去蹦跳；`d8e3dde` 地图导航；`4cb9ac5` 木器铺。基线 `db3eb63`，检查设施 `52565b8`。
- 家具主动互动桌面/375px 逐一操作浇花、坐下、打盹、读书、摇摆，五条发现均记录，余额保持500，无页面异常/溢出；证据 `home-interactions-{1280,375}.png` 和 `home-verification.txt`。这是固定本地家具 fixture，不读取用户存档。
- 地图截图与状态已补含木器铺的五场所版本，桌面/375px 主路径均可进入返回。三类具名场景重置入口及截图脚本保留，试验台仅本地使用。
- 已恢复原主站 BuddyFace、3D 小芽及 face-paint，暂停形象实验不影响已完成的去蹦跳。

## 下次该怎么做
1. 形象以用户认可的视觉参考为准。数值连续、测试通过只能证明动画工程正确，不能代替眼睛与整体观感的视觉验收；本次白眼方案不可直接接回。
2. 先在独立试验台展示静态各情绪与中间帧，形象认可后才共用到 2D / 3D。主站原参数与实验参数同在 emotions.js，但消费者和绘制模块已隔离。
3. 导航入口只加地图场所；相应补具名场景与375px标签避让验证。交互测试用稳定场所标识，不依赖桌面/手机显隐不同的装饰箭头。
4. 装修规则继续调用 game/room.js 与 store 门面；样本可扩展但价格不要复制到视图。用空余额、首次、连续扩建到上限、取消、刷新检查真实界面。
5. 页面钩子只读信息可保留；fixture 重置严格限定本地开发 + ?qa，正式 bundle 不得带测试存档或资金重置逻辑。发布前检查试验台没有进入 dist。
最终 `npm test` 29/29 通过；`npm run build` 成功，保留原 three.js 分包大小提示（553.71 kB，非错误）。已检查生产输出无 `emotion-lab.html`、测试存档键和 fixture 重置逻辑。发布只包含前三项功能，实验源码供后续交接。
