# 2026-09 执行记录

## 执行合同
按已批准顺序：去蹦跳 → 地图唯一导航 → 木器铺 → 独立表情试验台验收后集成。每项独立提交，保留 v3 存档和经济规则。最终同步 GitHub / Sites。

## 基线与检查设施
- 基线 `db3eb63`：原样纳入已有 AGENTS 与两份用户需求文档；26 项测试通过。
- 开发服务器 `?qa` 启用独立 `kuangye.qa.v3`，不读取正式或旧版存档；天气请求关闭。
- `window.__KUANGYE__.snapshot()` 读取场所、导航、情绪、房屋及视口；本地 QA 才提供 `reset(name)`。
- 具名场景：`map-navigation`、`woodshop`、`emotion-lab`。截图与状态写入 `output/playwright/`，逐项补充结果。
- 不在生产网址执行测试。独立试验台将有自己的状态钩子和重置操作。

## 1 · 去蹦跳
移除空地点击寻路、随机游走、移动时高度与缩放蹦跳。摸摸只触发表情；音乐互动保留摇摆，去掉上下跳跃。家具主动互动保留平滑靠近定位和坐下/打盹/浇花/读书。
验证：27 项测试通过；新增模拟时间检查静止 20 秒与五类家具动作，余额不变。本地浏览器房间加载无报错，截图 `output/playwright/idle-home-1280.png`。旧引导文案一并清理。

## 2 · 地图唯一导航
移除顶部/底部多入口及地图旁重复入口。子页统一回地图，旧 hash 继续兼容，未知/空 hash 回地图；成长线保留从任务/手记第二击进入。长期规则写入 AGENTS。
手机场所标签曾重叠，增加边缘限制、44px 触区和标签避让。复放脚本第一次因桌面箭头参与 accessible name 导致定位超时，改为稳定场所标识，桌面/375px 四个场所进入返回及无溢出检查全部跑通。
证据：`output/playwright/map-navigation-{1280,375}.png`、`navigation-verification.txt`；命令 `node scripts/run-browser-qa.mjs navigation`。
