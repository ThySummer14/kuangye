# 行动入口切片 · 2026-09-21

基线 e63f7f9，工作区干净，可回退。本轮小范围修复，不推倒重做。

现状：QuestView默认today仍在搜索时过滤E/D及成长线首阶段，搜得到的范围与“搜索一件想做的事”不一致；接取后卡片移至上方但焦点未跟随；累计进度缺少任务单位。

改为：有搜索词时跨today/all搜索，仍尊重用户选中的领域、已接取与已完成不可重复规则；去掉首尾空白。无搜索结果提供清空筛选动作。接取成功后将焦点和视口移动到对应在手任务，卡片提示在生活中完成再回来记录。累计进度展示声明中的单位。

验证：纯函数验证搜索可找到高阶、领域约束、已接取/已完成过滤与默认推荐不变；浏览器1280/375验证高阶搜索、空结果恢复、接取聚焦与键盘可用、无溢出。复放journey与日夜mascot，npm test/build通过。无新任务文案、无奖励通道、无存档字段变更。新说明为操作指引，不是新增系列任务。

成功衡量限于可验证的交互：输入标题能找到相应既有任务；点击接取后无需手动向上寻找；进度有单位。不把这些结果夸大为已经证明用户真实行动率提升。
